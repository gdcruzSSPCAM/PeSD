@model IEnumerable<PeSD.Models.TypeDisability>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeDisabilityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeDisabilityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeDisabilityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeDisabilityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeDisabilityId })
        </td>
    </tr>
}

</table>
